```c
# include <stdio.h>

int get_factorial(int x, int y)
{
	int r;
	while( y != 0){
		r = x % y
		x = y;
		y = r;
	}
	return x;
}
int main(void)
{
	int x,y;
	int gcd;

	while(1) {
		printf("정수 2개를 입력하세요. (0 0 ))}
}
```